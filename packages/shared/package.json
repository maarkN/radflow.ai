{
  "name": "@radflow/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared contracts: NATS subjects, event envelopes and payload schemas",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "jest"
  },
  "dependencies": {
    "zod": "^4.0.14"
  },
  "devDependencies": {
    "@swc/core": "^1.13.3",
    "@swc/jest": "^0.2.39",
    "@types/jest": "^30.0.0",
    "@types/node": "^24.13.3",
    "jest": "^30.0.5"
  }
}
